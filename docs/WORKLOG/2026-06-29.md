# WORKLOG 2026-06-29

## 今日やったこと

### バグ修正

#### つぶやき2重表示バグの修正
- 原因：`say()`でinsert中にポーリングが走ると、shownIdsにidが未登録のまま同じメッセージを拾って表示
- 修正：`pendingMsgContents`Setを追加し、送信中テキストをポーリング側でスキップ
- コミット：`fix: 送信中メッセージのポーリング2重表示を防止（pendingMsgContents）`

### 新機能

#### 時間帯による空の色変化・夜の街灯点灯
- 夜明け（5〜7時）：ピンク/オレンジ、街灯点灯中
- 朝（7〜11時）：薄い青みがかった光
- 昼（11〜16時）：ほぼ素の背景
- 夕方（16〜19時）：オレンジ〜赤、18時から街灯点灯
- 夜（19〜5時）：深い藍色、街灯3本がグロー付きで点灯
- `startSkyTime()`関数を追加、1分ごとに更新

#### 「記録する」ボタンのDB保存実装
- `recordThis(btn, text)`を非同期化、Supabaseの`records`テーブルに保存
- 保存失敗時はボタンを元に戻してリトライ可能に
- SupabaseにrecordsテーブルのSQL実行（ユーザー操作1回）

#### 名前・キャラ・着せ替えの記憶と復元
- `loadUser()`関数追加：DOMContentLoaded時にusersテーブルから前回データを復元
- `selectItem()`に`saveUser()`呼び出しを追加：着せ替え変更時に即時DB保存

## 未完了タスク
- なし

## 事故・失敗
- なし

## 次のセッションへの申し送り
- 機能的には一通り揃っている状態
- 残り改善候補：オンライン人数表示（Realtime未有効のためPollingも検討）
- アイテム位置の微調整（キャラによってずれる場合あり）
